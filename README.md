# Oops_concept

Add classes A1, A2 and A3 with constructors that print messages; A3 is inherited virtually. Add class B that inherits A1, A2 and virtual A3 and logs its construction. Update main() to construct a B instance instead of printing Hello World!. Also add a new binary file 'first' to the repository (new executable).